% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{build_db_assignment}
\alias{build_db_assignment}
\title{Assign postal centroids to Dissemination Blocks and join GAF}
\usage{
build_db_assignment(
  centroids_csv,
  province_shp,
  db_shp,
  gaf_csv,
  output_dir = NULL
)
}
\arguments{
\item{centroids_csv}{Path returned by \code{\link[=build_centroids]{build_centroids()}}.}

\item{province_shp}{Path to the province boundary \code{.shp} from
\code{\link[=download_census_boundaries]{download_census_boundaries()}}.}

\item{db_shp}{Path to the DB boundary \code{.shp} from
\code{\link[=download_census_boundaries]{download_census_boundaries()}}.}

\item{gaf_csv}{Path returned by \code{\link[=download_gaf]{download_gaf()}}.}

\item{output_dir}{Required directory for output files.}
}
\value{
Invisibly, the path to \code{ontario_postal_gaf_rollup.csv}.
}
\description{
Validates that centroids fall within the Ontario boundary, assigns
each to exactly one 2021 Dissemination Block via point-in-polygon,
and joins the Geographic Attribute File for higher geographies.
}
\examples{
# Consumes the census boundary and GAF downloads, so it is not run
# automatically.
\donttest{
if (interactive()) {
  cache <- tempfile("opcc-build")
  boundaries <- download_census_boundaries(cache_dir = cache)
  gaf_csv <- download_gaf(cache_dir = cache)
  centroids_csv <- build_centroids(
    download_nar(cache_dir = cache),
    download_geonames(cache_dir = cache),
    output_dir = file.path(cache, "centroids")
  )
  build_db_assignment(centroids_csv, boundaries$province, boundaries$db,
                      gaf_csv, output_dir = file.path(cache, "rollup"))
}
}

}
