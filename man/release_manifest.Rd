% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opcc.R
\name{release_manifest}
\alias{release_manifest}
\title{Read and verify a release manifest}
\usage{
release_manifest(
  vintage = "2026-06-26",
  cache_dir = tools::R_user_dir("OPCC", "cache"),
  offline = FALSE,
  level = c("DB", "DA")
)
}
\arguments{
\item{vintage}{A value returned by \code{\link[=list_vintages]{list_vintages()}}.}

\item{cache_dir}{Directory for the small, verified, actively managed
runtime cache.}

\item{offline}{Require an already cached verified file.}

\item{level}{Geography level, \code{"DB"} or \code{"DA"}.}
}
\value{
A parsed JSON list.
}
\description{
Read and verify a release manifest
}
\examples{
list_vintages("DB")
cache <- tempfile("opcc-cache")

\donttest{
# Downloads and checksum-verifies the release manifest, so it needs
# network access and is not run automatically.
if (interactive()) {
  manifest <- release_manifest("2026-06-26", cache_dir = cache)
  names(manifest)
}
}

}
