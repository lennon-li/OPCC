% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_census_boundaries}
\alias{download_census_boundaries}
\title{Download Statistics Canada 2021 census boundary shapefiles}
\usage{
download_census_boundaries(cache_dir = NULL)
}
\arguments{
\item{cache_dir}{Build cache directory.}
}
\value{
Invisibly, a named list with \code{province} and \code{db} paths to the
extracted \code{.shp} files.
}
\description{
Downloads the province/territory and Dissemination Block boundary
shapefiles. Skips downloads if already cached.
}
\examples{
# Downloads two Statistics Canada boundary zips (hundreds of megabytes),
# so it is not run automatically.
\donttest{
if (interactive()) {
  boundaries <- download_census_boundaries(cache_dir = tempfile("opcc"))
  boundaries$province
}
}

}
