% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{export_postal_points}
\alias{export_postal_points}
\title{Write a postal-code point shapefile for an OPCC join}
\usage{
export_postal_points(
  postal_code,
  joined,
  zipfile,
  vintage = "2026-06-26",
  cache_dir = tools::R_user_dir("OPCC", "cache"),
  offline = FALSE
)
}
\arguments{
\item{postal_code}{Character vector of postal codes.}

\item{joined}{A data frame produced by joining records to an OPCC
correspondence, carrying the \code{opcc_postal_code_col} and \code{opcc_dauid_col}
attributes set by the app. Plain \code{opcc_postal_code} and \code{DAUID} columns are
used when those attributes are absent.}

\item{zipfile}{Path of the \code{.zip} archive to write.}

\item{vintage}{Centroid artifact vintage.}

\item{cache_dir}{Cache directory for downloaded artifacts.}

\item{offline}{If \code{TRUE}, use only already-cached artifacts.}
}
\value{
The \code{zipfile} path, invisibly, or \code{NULL} when no points were
available to write.
}
\description{
Builds a point layer for the supplied postal codes, one feature per unique
code, carrying the dissemination-area roll-up from \code{joined} as attributes,
and writes it to a zipped ESRI Shapefile. The points are the same
source-qualified postal centroids the OPCC Shiny app draws on its map, so a
script generated by the app reproduces exactly what the app displayed.
}
\details{
Codes with no point evidence cannot appear in the output; that is a property
of the evidence, not a failure. When no supplied code has any point
evidence, nothing is written and the function returns \code{NULL}.
}
\examples{
# Writing the shapefile downloads a versioned centroid artifact, so this
# runs only in an interactive session.
\donttest{
if (interactive() && requireNamespace("sf", quietly = TRUE)) {
  joined <- data.frame(opcc_postal_code = "M5S1A1", DAUID = "35200001")
  export_postal_points(
    "M5S1A1", joined, file.path(tempdir(), "opcc_postal_points.zip")
  )
}
}
}
